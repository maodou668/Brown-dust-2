# 本地化迁移交接（2026-07-05 用户拍板：全面本地化）

> 给接手的本地 Claude Code 会话：先读 `CLAUDE.md`（经理规则）→ 本文件（当前进度）→ 按需翻《游戏开发说明书》《本作开发档案》。

## 为什么迁移
云端会话累计发生 13+ 次容器回滚（工作靠远端仓库自愈未丢失），2026-07-05 又遇 GitHub Pages 部署平台连续拒收（根因=整仓上传产物过肥，已修：pages.yml 只上传 _site 站点文件）。用户决定改为本地开发，摆脱容器回滚与部署等待。

## 本地开发要点
- 仓库：https://github.com/maodou668/Brown-dust-2 （原名 Maodou，已改名）
- 开发分支照旧：`claude/brown-dust-2-dev-ma6cog`，不开 PR。上线仍走 GitHub Pages（push 触发 pages.yml）。
- 首次进场：`npm install`（build 与 devtools 依赖 @napi-rs/canvas、playwright 等）。
- 本地预览：任意静态服务器起在仓库根目录，如 `npx http-server -p 8080`，浏览器开 `http://localhost:8080/index.html`。**本地预览改完即见，不用等部署**；对外发布才 push。
- 上线循环、数值闸门、提交格式等铁律全在 `CLAUDE.md`，原样有效。

## 当前进度（截至 v245，已全部推送）
1. **大图路线定档**：整图直出 + 用户手绘图层制（拼装案已作废），细节见《本作开发档案》§6.0.5 末段。
2. **标定工坊 v2**（js/bmedit.js）已上线：`?bmedit&map=town_day2` 在用户的无烟新图上画碰撞层/遮挡层。本地就是 `http://localhost:8080/index.html?bmedit&map=town_day2`。
3. **town_day2.png**（2744×1568，用户 Gemini 无烟版，菱形水印+签名已修）已入库，bigmap.js 有占位条目。
4. **游戏当前仍跑旧图 town_day**（1792×1024，v242 几何），等用户画完图层再换底。

## 挂起事项（按优先级）
1. **等用户在工坊画** `town_day2_mask.png`（碰撞：红=不可走/绿=修正）和 `town_day2_occ.png`（遮挡件：颜色编码脚线 r*256+g=baseY）→ 拿到后在 bigmap.js 的 town_day2 条目接 `maskImg/maskOnly:true/occImg`，把 story2.js 第一章底图换成 town_day2，spawn/相机/glows/烟囱烟锚点/鸡圈区域全部按新图坐标重标，actorScale 0.92 需实测校准（门高≈1.15人高为准）。
2. **领主宅左烟囱残留一缕画死烟**（新图 ~x1180-1225,y55-115）：用户说烟的方案他整体定，未获指令前不动。
3. 换底后跑 walktrace 实走闸门（注意路线要按新图重写，world 尺寸是动态读的）。

## 用户使用习惯（重要）
- 用户无编程/美术基础，是"投资人"角色；确认理解→给方案→等拍板→开工→自测带证据汇报。
- 生成类付费调用（PixelLab/RD）先报批；美术接入走 artgate + 目视闸门；场景生产走四步制（施工图→效果图→确认→施工→验收）。
- 新东西当场回写两份文档（说明书=通用法，档案=本作实例值），和代码同 commit。
