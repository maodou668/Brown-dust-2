# 通用游戏美术资产生产流水线（PixelLab · 任意游戏 / 任意画风 / 全资产类型）

> **一个文件管全游戏美术资产生成**。无论角色、怪物、地图、UI、特效、立绘——方法论、闸门、风格档案都在这。
> **与具体游戏解耦**：换游戏/换画风时**不改方法论，只换 §1 风格档案**。本作（Brown Dust 2 风横版 RPG）只是它的一个实例，
> 实例的硬编码细节（锚点是谁、各角色外观、bbox 数值）见 `ART_PIPELINE.md`，**不要写回本文件**。
>
> **文档生长方式**：目前仅「角色」已实战跑通（§A，详尽）。其余资产类型（§B–§G）已搭好骨架并标注该用哪个 PixelLab 工具、
> 复用哪些通用闸门；**等真正做到那一类时，把实战经验/踩坑/脚本补进对应章节**（每章末尾有「📝 实战补充区」）。

---

# 第一部分 · 全资产共享地基

## §0. 核心信条（做任何美术资产都成立）
1. **一致性靠"锚点派生"**，不靠"逐个从零生成"。同一族资产（一队角色 / 一套地块 / 一组 UI）先做一个精当锚，其余从它派生 → 共享画风。
2. **画风是参数，不是常识**。把画风固化成「风格档案」（§1），每个 prompt 都套同一份；换游戏=换这份档案。**没有放之四海的提示词，只有匹配本作画风的提示词**（柔和动漫禁 `black outline`；复古像素反而要 `bold black outline`）。
3. **闸门先于规模**。每件资产先过该类型的目视/自测闸门、人工验收，**再做下一件**；绝不批量。坏母图/坏底图绝不进下一步。
4. **生成有随机性 → 用兜底阶梯收敛**，不要无限 re-roll（一般 ≤2 次不行就换法/兜底）。
5. **归一化是接入的前提**。尺寸/锚点/调色不齐，必须程序化归一到统一契约再进引擎。
6. **自测能跑就别靠眼缘**。能脚本量化的（脚漂移、朝向、瓦片接缝、技能配对）就写成闸门脚本。
7. **PixelLab 并发约 10 槽硬上限**：塞满后新 job 静默排队/丢失。**一次 fire ≤4 个**，回收再发；产物下载后逐件核对，缺件单独重发（别整套重发浪费额度）。出图 ~5–8 分钟，后台 `sleep` 等待再轮询。

## §1. 风格档案（每个游戏填一份 —— "适配任意游戏"的关键）
> 新游戏开工第一步：复制本表逐项填成你这个游戏的值。后续所有 prompt / 脚本参数都引用它，填完方法论原样套用。
> 上半部分是**全资产通用画风**；下半部分按资产类型补充各自的尺寸/视角参数。

**A. 通用画风（所有资产共享一份）**

| 维度 | 说明 | 你的游戏填这里 |
|---|---|---|
| 描边 outline | `selective outline`(柔和) / `single color black outline`(复古) / `lineless` | |
| 明暗 shading | `high detail, soft smooth shading` / `low detail, hard shading`(块状复古) | |
| **风格后缀（固定词串）** | 每个 prompt **结尾固定**贴这串锁画风。柔和动漫=`soft smooth shading, no black outline, clean lineart`；复古像素=`crisp pixel art, bold black outline, limited palette, retro 16-bit` | |
| 调色板 palette | 是否限定主色板 / 冷暖基调 / 元素配色规则 | |

**B. 各资产类型参数**

| 资产类型 | 关键参数 | 你的游戏填这里 |
|---|---|---|
| 角色/怪物(§A/§B) | 画布 size、视角 view、方向数、头身比、道具策略、动画集 | |
| 地图/瓦片(§C) | 瓦片像素、视角(topdown/iso/side)、瓦片集结构 | |
| UI(§D) | 控件清单、统一边框/底纹风格、@1x 像素尺寸 | |
| 特效(§E) | 帧数、特效风格词、命中帧标定法 | |
| 立绘/头像(§F) | 半身/头像裁切框、输出尺寸 | |
| 字体(§G) | 字号、字符集、像素风格 | |

> ⚠️ **风格后缀是最常翻车的一项**。先拿锚点试 2–3 版后缀挑最贴的固化下来；之后**所有资产类型都复用这同一串**，保证全游戏画风统一。

---

# 第二部分 · 按资产类型分章

## §A. 角色（✅ 已实战跑通 —— 完整流程）

### A.1 锚点与派生（一致性根基）
1. **做锚**：选代表性角色用 `create_character`（mode v3，按风格档案）精心生成，反复挑到头身比/画风/骨架满意。**锚定全队上限**。
2. **派生其余**：`create_character_state(character_id=<锚>, edit_description="Transform into: <外观>, <风格后缀>", use_color_palette_from_reference=false)` → 继承骨架/头身比/画风。
3. ❌ 不要逐个 `create_character`：v3 忽略 `proportions`，头身比逐个漂移 = "画风不统一"头号根因。
4. 外观描述写身份特征（发色/发型/瞳色/服装/配饰/元素宝石/表情）；**别写** "small"/"front view symmetric"（触发大头/对称偏置）。

### A.2 🚦 母图姿势闸门（铁律 · 出母图后、做任何动画前必过）
动画每帧 frame0 = 母图该向旋转；母图错则整套继承错、必返工。先免费看旋转，逐向目视：
- **east/west = 正侧面**：只露一条腿、一条胳膊、侧脸；露第二条腿/正脸=带角度，**不合格**。
- south/north=正前/正后；斜角=真 3/4；头身比/画风与锚一致。
- 不合格 → 锁法A 换 seed 重派生再检测（最多 2–3 次）。**侧面单独生成行不通**（漂成正面矮人/糊块），老实重派生母图。

### A.3 动作生成（`animate_character mode=v3`，镜像省额度：对称向只做一半）
- **idle（循环）**：`"standing idle, breathing gently with subtle sway"`；背向斜角易抖腿→两斜角源都生成取脚稳者镜像。
- **walk/run —— 先建"迈步状态"再循环**：`create_character_state(母图,"mid-stride walking pose…")` 后在该状态做 `walking_cycle`（直接做循环会腿朝别处/转头）。
  - east/west 走路常漂成 3/4 → 在**基础母图**上单独做纯侧面 `walk_side`，归一时覆盖 run 的 E/W。
  - 迈步状态偶发把斜角朝向画反 → 靠 A.5 走路朝向闸门兜底。
- **cast/attack（一次性，少数角度）**：描述**直接写技能内容**（"casting a blazing inferno"），元素特效烤进角色帧；命名固定前缀便于归一器分类。

### A.4 组装归一
1. **主归一器**：所有向所有动作统一到固定 bbox+画布+锚点（脚底对齐地线），居中；idle/run 镜像在此用对称表完成。
2. **全员身体高归一**：按身体高（脚→头顶、**排除光环/帽缨**）缩放到统一值+高画布；头盔/宽体/光环角色头顶检测易误判 → 提供**显式体高覆盖**。
3. 渲染画整幅、脚对齐地线，不裁顶。输出 `<char>_field/` + `manifest.json`，引擎注册 sprite。

### A.5 🚦 走路朝向闸门（组装后必过）
逐向把 run 与 idle 并排对照，每向 run 朝向必须和 idle 一致（参考 `scripts/rundiag.js`）。接反/带角度→重生成该向 walk-state（换 seed）或水平镜像修正。

### A.6 自测闸门 + 兜底阶梯
- **脚漂移闸门**：idle 各向脚部逐帧位移全向 ≲1.5px（如 `fd8.js`）。⚠️ 达标≠不抽腿：背向斜角两腿对称交换质心不动也会过 → **额外目视背向斜角横条**确认脚钉地。
- **兜底阶梯**：v3 背向斜角怎么 roll 都残留位移 → 最多 re-roll 1–2 次，还抖就**合成呼吸**（取脚钉地静态帧、只对上半身做垂直起伏、腿脚锁死、脚漂移 0px）。
- **游戏内配对**：真实场景触发每个技能，截图验证动作↔技能↔VFX 不串不缺（`skillcap.js`）。

### A.7 技能表现 = 两层模型（见 §E 特效详述）
角色层（cast 动画烤入元素光）＋ 特效层（独立 burst 打目标）＋ `impactFrame` 握手（伤害缓存到命中帧 flush）。

### A.8 单角色完整清单（复制即用）
```
[ ] 风格档案已填(§1)；锚点已做且验收(A.1)
[ ] 锁法A 派生母图(A.1)
[ ] 🚦 母图姿势闸门 逐向合格(A.2)            ← 不过换 seed 重派生
[ ] idle / walk-state→walk / cast 生成(A.3)   ← ≤4 job/批,缺向单独 _v2 重发
[ ] 组装归一(A.4) + east/west 侧面源覆盖
[ ] 🚦 走路朝向闸门 run vs idle 逐向对照(A.5)
[ ] 脚漂移闸门 + 目视背向斜角;抖腿走兜底阶梯(A.6)
[ ] 全员身体高归一;目视全队同框等高(A.4.2)
[ ] 技能两层接入 + 游戏内配对验证(A.7/§E)
[ ] 构建/版本/部署 → 截图发用户验收 → 再做下一个
```

📝 **实战补充区（角色）**：已在 `ART_PIPELINE.md` 沉淀本作 16 人全部踩坑与脚本，做新游戏角色时把通用经验回填这里。

---

## §B. 怪物 / BOSS（⬜ 待实战 · 骨架已就位）
- **本质 = 角色**：用 `create_character` + `animate_character`，**§A 全套方法论/闸门直接复用**（母图姿势闸门、走路朝向闸门、脚漂移、归一）。
- **一致性策略不同**：怪物之间形态差异大，**不强求全员一个锚**；改为**按阵营/系列各设一个锚**（如"亡灵系"一锚、"机械系"一锚）派生同系怪，保证同系画风统一、跨系自由。
- **动画集按行为定**：通常 idle + move + attack +（受击 hurt / 死亡 die）。BOSS 多加大招前摇/阶段切换。
- **体型**：大型 BOSS 用更大 size，归一时**单独一档身体高**（别和小怪/角色塞同一归一值）。
- 静态场景怪/陷阱可用 `create_map_object`（见 §C）。

📝 **实战补充区（怪物/BOSS）**：待补 —— 阵营锚清单、各怪 size 档、攻击命中帧与伤害握手、BOSS 多段。

---

## §C. 地图 / 瓦片（⬜ 待实战 · 骨架已就位）
- **PixelLab 工具按视角选**：
  - 俯视 RPG → `create_topdown_tileset`
  - 横版平台 → `create_sidescroller_tileset`
  - 等距 → `create_isometric_tile`
  - 高质量瓦片 → `create_tiles_pro`
  - 地图上的道具/物件（树、箱子、雕像）→ `create_map_object`（可加 `create_object_state` 出变体/开关态）
- **风格档案复用**：瓦片同样贴 §1 风格后缀，保证和角色同画风。
- **通用闸门 → 瓦片版**：
  - 🚦 **接缝闸门**：把瓦片平铺 2×2/3×3 渲染，目视边缘**无缝、无明显重复纹理**。
  - 🚦 **同框闸门**：地块与角色并排渲染，确认透视/明暗/像素密度一致（别地块写实、角色卡通）。
- **接入**：瓦片切图 + 图集 + 碰撞/图层约定（按引擎的地图格式）。

📝 **实战补充区（地图/瓦片）**：待补 —— 瓦片尺寸定档、图集打包脚本、接缝/同框闸门渲染脚本、自动平铺预览。

---

## §D. UI 资产（✅ 已实战跑通 —— 全套套装 + 全局接入）
> 本作已用 PixelLab 生成整套 UI（面板/标题栏/三级按钮/标签页/条框/头像框/稀有度框/功能·资源·元素图标）并全游戏接入。下为踩平的坑与定法。本作设计系统见 `dev/UI_DESIGN.md`。

### D.1 工具与批量（省额度是第一原则）
- **`create_ui_asset`**：面板/按钮/边框/条框/标签页等"控件"。一次 20–40 generations，**出一张**（非候选制），所以提示词要一次到位。
- **`create_1_direction_object`**：图标。`size` 决定候选数（≤42→64，≤85→16，≤170→4）——**一次调用内的 16 个候选不额外收费**，`get_object` 看候选挑最佳。
- **🟢 批量定法**：`item_descriptions` 数组在**一次调用**里出**多个不同**图标（如一次出火/水/风/地/光/暗 6 元素），是省额度的关键。别一个图标一次调用。
- **能 CSS 就别生成**：稀有度辉光、凹槽物品格、三级轻钮、红点/NEW、星级、卡片染色、平滑底辉——纯 CSS 兜，绝不烧 generation。已生成的图能复用就复用（如从角色表里裁一个圆头像框）。

### D.2 🚦 像素感闸门（铁律 · 本作返工教训）
- **分辨率 = 像素颗粒感**。`create_ui_asset` 出图**越大越"高清"**，颗粒越细 → **不像像素风**。把边框做成 384×512 + "engraved/embossed/filigree/fine detail"提示词 → 出来是 HD 雕花，和像素游戏违和（用户一眼看穿）。
- **定法**：UI 控件**尺寸对齐本套其他件**（本作 panel≈358、button≈262、tab≈215、frame 用 **192×256**）；提示词写 **`chunky pixel art, bold thick, limited palette, hard pixel edges, flat shading, no anti-aliasing`**，**别写** `engraved/embossed/filigree/fine/HD/smooth`。
- CSS 侧所有 PixelLab UI 元素加 **`image-rendering: pixelated`**，缩放时保持硬边不发虚。

### D.3 九宫格接入（border-image）
- 边框/面板/按钮/条框 → CSS `border-image: url('../art/...') <slice> fill stretch` + `border-width`。`slice` = 源图四角不拉伸区的像素宽。
- **空心框**（稀有度框/头像框）：`fill` 仍会铺中心 → 若要透出内容，去掉 `fill` 或让中心透明。
- **居中陷阱**：border-image 的边框吃掉内容框（如 30px 钮 −2×7px 边 = 16px 内容框）→ 里面的图标必须 **flex 居中 + 尺寸 ≤ 内容框**，否则溢出/偏位（系统图标返工教训）。

### D.4 🚦 路径闸门（线上必测 index.html）
- **外链 `css/style.css` 的 `url()` 相对 `css/` 目录解析** → 引 `art/` 必须写 **`url('../art/...')`**；`build.js` 内联进根目录 `game.html` 时自动去掉 `../`。
- **自测必测 `index.html`（线上入口），不是 `game.html`**——内联版路径不同会**掩盖** 404 路径 bug（"打开链接没 UI"的根因）。

### D.5 字体全局（像素字体）
- **`body` 的 `font-family` 不会传到 `<button>/<input>/<select>/<textarea>`**（表单元素默认不继承）→ 必须显式 `button,input,select,textarea{font-family:inherit}`，否则功能按钮文字回退系统字、与像素 UI 违和（本作真实漏接）。
- 中文像素字（如 Zpix）用 `pyftsubset` 按**实际用到的字**子集化（→ ~68KB woff2）。**缺字会按字逐个回退**系统字 → 子集必须覆盖所有 UI 文案，接入后目视确认无回退字形。

### D.6 颜色/纹理陷阱（本作两次返工）
- **别用角色主题色染套框卡的空心中心**：立绘 `object-fit:contain` 会留白，主题色从留白处透出成"色条"（红条 bug）。改**暗中性底**（与立绘深色背景融合）。
- **别在卡背景铺斜条 `repeating-linear-gradient`**：若卡面立绘渐变 fade 到 transparent，条纹会透上来铺满整卡、显脏。**平滑径向微辉**即可。

### D.7 图标可读性
- 图标按**形状**辨识、不靠颜色（火=一团火、水=水滴、风=旋风…），简约一眼可辨。占位 emoji（顶栏系统钮等）必须全部换掉，别遗留。

### D.8 接入闸门（部署前）
- 无头浏览器（Playwright，**横屏 960×520**，开局跳过剧情）截图**前后对比**；3× 缩放细看边缘锐度/图标居中/无 emoji 泄漏。
- 部署循环：bump `ASSET_VER` + 全 `?v=N` → `build.js` → commit/push → 轮询线上 `ASSET_VER`。

---

## §E. 特效 VFX（◐ 角色技能特效已实战 · 通用化）
> 技能表现 = **两层模型**（与画风无关的通用结构，本作已跑通 21 个特效）：
1. **角色层（cast 动画）**：施法动作+元素光烤进角色帧（§A.3）。贴在角色身上。
2. **特效层（独立 burst）**：`create_1_direction_object`（size 64，16 候选）→ 挑（`get_object` 看候选，`select_object_frames` 提升）→ `animate_object`（v3，frame_count 12 存 13 帧）→ 下载切帧。打在目标身上。提示词只写**主体 + §1 风格后缀**（柔和画风**绝不**写 `black outline`/`low detailed`）。
3. **握手（动画↔逻辑同步）**：战斗逻辑瞬时算完且权威；伤害视觉表现（掉血/飘字）**缓存**，等特效 `impactFrame`（最亮爆发帧）再 flush → 画面与扣血严丝合缝。
4. **标定 impactFrame**：脚本统计每帧亮度/不透明像素峰值，峰值帧即命中帧。
5. **配对**：每技能 → 一段 cast 动画 + 一个 burst；引擎建 技能id→{cast,burst,时序,元素色} 注册表。素材没到位用程序化兜底（握手逻辑相同），到位后无缝替换零改代码。
6. 非战斗特效（拾取闪光、环境粒子、传送门）同 `create_1_direction_object→animate_object` 路子。

📝 **实战补充区（特效）**：本作 fx 流程细节见 `art/05_pixellab/fx/README.md`；新游戏把命中帧脚本/兜底特效经验回填这里。

---

## §F. 立绘 / 头像 / 半身（⬜ 待实战 · 骨架已就位）
- **来源两条路**：①用户给的高清动态/静态立绘 → 程序化**裁切**头像/半身（定裁切框）。②PixelLab `create_portrait_character` 直接生成像素头像（与角色同身份描述+§1 风格后缀，保证和小人同款脸）。
- **一致性**：头像/半身的脸要和 field 小人**同一人** —— 用同一份外观描述派生。
- **通用闸门 → 立绘版**：🚦 **同人闸门**：头像 + field 小人并排，确认发色/瞳色/配饰一致。
- **接入**：裁好的头像/半身写进角色数据的 `art` 字段 + 立绘视频接 splash。

📝 **实战补充区（立绘/头像）**：待补 —— 裁切框尺寸、视频抽帧脚本、portrait 与 field 对脸校验。

---

## §G. 字体 / 其他（⬜ 待实战 · 占位）
- 像素字体：`create_font`（字号/字符集/风格按 §1）。
- 其他一次性资产（loading 图、徽章、成就图标）：按是否需要方向/动画，选 `create_1_direction_object` / `create_map_object` / `create_ui_asset`。

📝 **实战补充区**：待补。

---

# 第三部分 · 移植到新游戏
1. 复制本文件 + 通用脚本（归一/自测/接缝/裁切等，把硬编码路径与 bbox 改成参数）。
2. **填一份新的风格档案（§1）** —— 唯一需要为新游戏定制的东西。
3. 做对应资产类型的锚，跑通一件全流程并验收，确认风格档案/闸门都对。
4. 之后逐件派生量产，方法论与闸门原样不变。
> **风格深化**：同一套方法论下，靠风格档案的描边/明暗/调色/风格后缀四项，就能从复古像素切到柔和动漫、从 8 向横版切到 4 向俯视，无需改流程。
> **文档维护**：每开一类新资产，就把该类的 📝 实战补充区填实（工具、闸门脚本、踩坑、接入约定），保持"一个文件管全美术"。
