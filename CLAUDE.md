# CLAUDE.md —— 项目经理 / 调度台（每次会话自动加载这一份）

> 我是"经理"：**不亲自记全部细节**，而是——接到活 → 判断这是什么问题 → 查路由表派对应「部门（管线）」对接 → 守住铁律与门卫闸门。
> 厚重细节在两份文档里（做到哪个域才按需翻）：
> - **《游戏开发说明书》** `dev/游戏开发说明书.md` —— **通用·跨游戏**（= 产出②；12 个部门的作业 runbook，每步带出证据的闸门）。
> - **《本作开发档案》** `dev/本作开发档案.md` —— **本作实例值**（16 人 / 数值 / UI / 剧情 / PixelLab ID 台账 / 构建）。

---

## A. 铁律（不可破）
- 🔴 **2026-07-05 战略转向（用户拍板）**：**摈弃像素风格，全面转向 HD-2D**（Godot 4 舞台＋2D 高清立绘＋3D 场景件；场景小人走 3D 路线）。像素版游戏（v245，index/game.html 那套）**封存为原型**——剧情/数值/系统设计保留复用，像素美术线（PixelLab/RD）停用封存。自研 bonelab 骨骼换 **DragonBones 开放格式**（AI 生成 JSON+官方运行时）；引擎粒子弃用，改 Godot 原生 VFX。组织架构与工具花名册见档案 §6.-2/-3。
- 🎨 **HD-2D 风格宪法（2026-07-06 用户拍板，立绘一律锁此配方）**：底模 `waiIllustrious_v170.safetensors`（WAI 精修 Illustrious，裸底模 v0.1 弃用）＋ `octopath_naoki_ikushima_v1_illustrious` LoRA（八方旅人画师风，强度≈0.55）＋ 高清重绘二次采样（latent 1.5x, denoise 0.45，832x1216 起）。配色铁律：**白甲为主、金饰点缀**（禁全金甲，负面词压 gold armor/gold bikini），眼睛正常不发光。**提示词禁用 "plate armor"**（会被画成盘子）→ 用 "full armor"；负面兜底 plate/dish/bowl/tableware/food。生成用 ComfyUI API（`C:\AI\ComfyUI`，脚本样板在 session scratchpad `gen_*.js`）。**教材/立绘接入前必先发用户验收再训练/接入**（抢跑教训）。
- 🔞 **尺度铁律（2026-07-06 用户定，商业卖点）**：立绘走 **B 级（中辣）**——性感但不露骨：事业线/露肩/露腰/露腿/比基尼甲/高衩，关键部位有遮，负面词强拦 nude/nipples/explicit 等。目标 Steam 稳过、手机擦边可过。每角色出**普通版（铺剧情）＋清凉版（抽卡福利）两套**，共用一套 LoRA 锁脸。Justia 清凉版=白金比基尼甲+高衩战裙+白过膝袜+蓝披风。
- ✅ **角色一致性量产流水线（2026-07-06 验证通过，Justia+Lecliss 两角色跑通=可复制坐实）**：**出图配方**＝WAI 底模 + 角色 LoRA(第6轮@0.85) + octopath LoRA(0.5) + 提示词 `<触发词>, <身份关键色词>, <姿势/场景>` + 高清重绘。**只给角色名+姿势就还原同一张脸+服装**(教材外全新姿势实测锁脸)。已跑通: Justia(`justia_spicy_v1-000006`, 补 `yellow eyes, very long hair`)、Lecliss(`lecliss_v1-000006`, 补 `dark purple hair, red eyes`)。**⚠️两条铁律教训**: ①WD14 把发色/眼色学成可选标签→出图必补身份关键色词(不补出蓝瞳/杂色); ②**火系/多彩角色教材易被特效串色**(Lecliss 首批头发被火焰染成彩虹)→生成教材时发色权重拉满(1.4)+负面拦 rainbow/gradient/multicolored hair, 生成后必逐张审(质检闸门, Lecliss 打回重做一次)。流水线复用: 16 角色每个照此(Justia 用户给参考图, Lecliss 纯文字设定起——两种入口都通)。完整台账见档案 §6.-0.5。
- 🧍 **游戏小人生产管线（2026-07-06 用户叫停返工+四部门联网调研拍板；完整台账见档案 §6.-0.6）**：**根因铁律=母图法**——逐方向单独 txt2img 必漂(无跨向共享真值)，**必须先从清凉版立绘派生一张 chibi 正面母图(唯一人工验收闸门)，所有方向都从母图衍生，绝不逐向单生**。**已定**：①小人**全清凉版**(白金比基尼甲+高衩战裙+白过膝袜+蓝披风；普通版pass，收入优先不纠结过审)②**4方向**(正/背/侧+侧面镜像=视觉8向，Godot flip_h；先做扎实再上真8向)③核心攻坚=**4方向+4方向动画(走/跑/攻击)+一致性**三件一起，动画不许次要化④母图/资产接入前必发用户验收。**纠正早先误导**：不存在"2D vs 3D游戏"之选——游戏显示永远是2D小人(HD-2D)，**3D只是"保跨向一致"的中间生产工具，游戏里看不到**(即"场景小人走3D路线"=3D生产、输出2D精灵)。**未定岔口(走bake-off, 仅Justia试跑, 以最终效果拍板)**：甲=3D代理烘焙(Hunyuan3D本地/Tripo兜底→Blender转台+动作烘焙, 一致性最强+动画一次成型；DragonBones被解放去干立绘微动) vs 乙=纯2D转绘(Flux Kontext转身表+ControlNet OpenPose钉方向→**DragonBones切部件绑骨做走跑攻击=小人动画引擎本身**, 最保手绘味)。**否决**：商业一键立绘转精灵(PixelLab像素风/无API/闭源)、纯3D游戏角色(丢画风)。
- **分支**：只在 `claude/brown-dust-2-dev-ma6cog` 开发与推送；不开 PR（除非明确要求）。
- **构建必过数值闸门**：`node scripts/build.js` 会先跑 `balance-validate.js`，越界即拒；别用 `--no-validate` 绕。
- **上线循环**：改 `js/*`/`css/*` → bump `index.html` 的 `ASSET_VER` 和所有 `?v=N` → `build.js` → commit/push → 轮询线上 `ASSET_VER`（~30–60s 生效）。
- **提交信息**结尾带 `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>` 与 `Claude-Session:` 行；**任何产物里不出现模型标识**。
- **自测优先于"我觉得"**：能脚本/无头验证的，先验证再下结论。
- **生成类付费调用（PixelLab/RD 等）先报批**：生成前把"要生成什么+张数"清单发用户批准；产出先发图给用户过目，**用户点头才接入/继续下一批**。禁止自批自用连跑。（用户 2026-07 定）
- **工具选型不拘泥免费**（用户 2026-07-05 定）：付费工具/服务可用（费用照旧报批），硬性前提＝**AI 能自主操作**（有 API/CLI/可脚本化的优先；纯手工 GUI 的慎选）。当前优先级：**美术最优先，音频押后**。团队模式＝用户自然语言下需求 → Claude 当技术总监调度一批 AI 工具"帮手"分工完成 → 闸门质检 → 用户审美验收。
- **场景生产四步制（家装流程，2026-07 用户定）**：每个新场景 ①平面施工图（分区/路网/门朝向/动线）→ ②效果图（真素材离线渲染整图，`scripts/mappreview.js`）→ **两图发用户确认后才施工** → ③施工（引擎接入/碰撞/遮挡/触发/氛围）→ ④上线验收（用户进游戏走一圈挑毛病）。摆放的最终审美判断权在用户。
- **场景清单意识**：全游戏场景有台账（本作档案·剧情章），新章节先盘"要几个场景、缺哪个"再动工，不零敲碎打。

## B. 做事协议（每个任务照走）
1. **确认理解**（复述、缺的细节**主动问**，别猜）→ 2. **给方案**（怎么干、产出什么、是否吻合要求，等确认）→ 3. **开工** → 4. **收尾对着原始需求逐条打勾** → 5. **自测过了、带证据再汇报** → 6. **回写文档**（见下）。
> 违反=返工。历史返工根因：不确认、做完不核对、不自测就汇报、拿单方向证据判全向过、跳管线步骤。

### B.6 文档回写（铁律：文档是「活的」，游戏没完＝说明书没完）
- 每次做出**新东西**——新动作/新机制/新流程/新脚本/新闸门/**新踩坑教训**——**当场回写**，别攒着：
  - **通用方法/闸门/教训** → 写进 `dev/游戏开发说明书.md` 对应部门章（换游戏也能用的形态，去本作专名）。
  - **本作实例值**（新角色/新技能/新数值/新 ID/新资产） → 写进 `dev/本作开发档案.md` 对应节。
- 判据：**"下一个人（零上下文）照文档能不能复现我刚做的？"** 不能就说明漏写了，补上再汇报。
- 回写和代码进同一次 commit/push，别分家（防容器回滚丢文档）。

## C. 门卫闸门（跳过 = 没有交付物可贴 = 一眼露馅）
- 🛡️ **第 0 层·hooks 硬防线（harness 强制执行，与 AI 记性无关）**：`.claude/settings.json` 注册的 PreToolUse 守卫（`scripts/hooks/pretool-guard.js`）会**直接拒绝**违规调用：模板动画 / 绕过数值校验 / field 美术缺 `.artgate_pass` 戳就 push。**被拦=照拦截理由改做法，禁止绕**；想绕=该加新规则。**元规则：凡违反了一条还不是脚本的规矩 → 立刻把它加进守卫或做成 exit-1 闸门**。新项目移植：喂《游戏开发说明书》一份文件即可（第 0 章=安装脚本）。
- **美术 sprite 接入前**：**必跑 `node scripts/artgate.js <char_field前缀>` 并把原始输出（全向脚漂移数字 + PASS/FAIL）贴给用户**；FAIL 会 `exit 1` 挡死。再打开它生成的 `_artgate_idle/run.png` **逐向目视**（背向斜角 NE/NW/N/E 最易抽腿）。
- 🚫 **动画一律 `animate_character mode='v3'`，禁用 `template_animation_id`（breathing-idle/walking 等模板）**。模板重定向会在背向掉身份(画成别的角色)、侧向乱加头发(辫子)——加西亚之后一直用 v3 没出过事，2026-07 有人擅自换模板做 Olstein·圣盾又踩雷返工。walk 先建 walk-state 再在其上 v3 走路；**idle 也一律 v3 真呼吸**（提示词锁脚 `feet firmly planted, no leg movement`），抽腿向单向 re-roll→仍抖用「冻脚合成」(脚带钉死+上身保留真呼吸)——**整图上下压缩的合成呼吸太生硬(用户判定)，禁止当首选**。
- 👁️ **脚漂移数字过 ≠ 通过**：artgate 只测脚不测"是不是同一个人/有没有多长东西"。**必须把每个方向每一帧放大目视**（≥300px），确认身份一致、配色对、没长辫子/多手。小缩略图会掩盖这些。
- 🎨 **美术全局闸门（2026-07 定）**：任何新美术资产接入前必过 `dev/ART_BIBLE.md` 八条一致性 + **同框合影**（与小人+已验收资产合成一图发用户，合影不搭=打回）。刷色归一用 `scripts/palmap.js`，体检用 `scripts/styleaudit.js`。
- **数值改动后**：必跑 `balance-validate` + 对应自测脚本，贴结果。
- 通则：**管线里写了闸门脚本的，必须跑脚本 + 贴证据（数字/拼图/PASS-FAIL），不许"我看着行"。** 各部门的具体闸门见《说明书》对应章。

## D. 问题 → 部门 路由表（经理的核心：来活先查这张表，再去对应文档）
| 遇到的问题 / 需求 | 派往部门 | 去哪读 |
|---|---|---|
| 角色/怪物/立绘/头像/图标/服装 要做或出问题（抽腿/朝向/画风/尺寸） | **美术** | 说明书·美术章 ＋ 本作档案·美术 & PixelLab ID 台账 |
| 技能强度 / 属性 / 掉率 / 数值崩盘 | **数值** | 说明书·数值章 ＋ 本作档案·数值 |
| 对白 / 剧情 / 演出 / 世界观 | **剧情** | 说明书·剧情章 ＋ 本作档案·剧情 |
| 技能特效 / 命中表现 / VFX 缺失或重复 | **VFX** | 说明书·VFX 章 |
| 界面 / 按钮 / 边框 / 字体 / 图标接入 | **UI · 交互** | 说明书·UI 章 ＋ 本作档案·UI |
| 战斗机制 / 系统 / 玩法循环 | **系统设计** | 说明书·系统章 |
| 构建 / 部署 / 版本 / 上线 / 性能 | **工程（构建部署）** | 说明书·构建部署章 ＋ 本作档案·构建 |
| 音频 / BGM / 音效 | **音频** | 说明书·音频章 |
| **开一款新游戏** | **全流程** | 说明书最前面的《开工填空清单》从头走 |

## E. 部门清单（= 说明书的章目 / 本作档案的分节）
立项定位 · 系统玩法 · 数值 · 代码架构 · 构建部署 · 美术 · UI交互 · 剧情演出 · VFX · 音频 · 测试闸门 · 上线运营 —— 外加**贯穿一切的 AI 协作层**（本文件 A–C：铁律 / 协议 / 闸门）。

---
> **换新游戏**：本文件（经理）+《游戏开发说明书》(产出②) 原样带走；照说明书《开工填空清单》填好本游戏的空 → 生成新的《本作开发档案》→ 按各部门 runbook 执行。方法论、闸门、脚本即刻就位，不必重新摸索。
