# 通用角色资产生产流水线（PixelLab · 任意游戏 / 任意画风）

> **这份文档与具体游戏解耦**。它把"用 PixelLab 量产一致、可动、可接入的角色序列帧"这件事，
> 抽象成一套**与画风无关的方法论 + 闸门**，外加一张**按游戏填写的「风格档案」**。
> 换个游戏、换个画风时：**不改方法论，只换风格档案**（§1）。
> 本作（Brown Dust 2 风横版 RPG）只是它的一个实例，实例细节见 `ART_PIPELINE.md`，**不要把实例数值写回本文件**。

---

## 0. 核心信条（换什么游戏都成立）
1. **一致性靠"锚点派生"，不靠"逐个生成"**。选一个角色做精当锚，其余全部从它派生 → 共享骨架/头身比/画风。
2. **画风是参数，不是常识**。把画风固化成一段"风格档案"，每个 prompt 都套同一份；换游戏=换这份档案。
3. **闸门先于规模**。每个角色一条龙做完、过自测、人工验收，**再做下一个**；绝不批量。坏母图绝不进动画。
4. **生成有随机性 → 用兜底阶梯收敛**，不要无限 re-roll。
5. **归一化是接入的前提**。原始帧尺寸/锚点不齐，必须程序化归一到统一契约。
6. **自测能跑就别靠眼缘**。脚漂移、朝向、技能配对都有可量化的闸门脚本。

---

## 1. 风格档案（每个游戏填一份 —— 这是"适配任意游戏"的关键）
> 新游戏开工第一步：复制下面这张表，**逐项填成你这个游戏的值**。后续所有 prompt / 脚本参数都引用它。
> 填完它，方法论（§2 起）原样套用即可。

| 维度 | 说明 | 你的游戏填这里 |
|---|---|---|
| **视角 view** | `low top-down`(横版/斜俯) / `high top-down`(俯视) / `side`(纯横版) / `isometric` | |
| **画布 size** | 16 / 32 / 48 / 64 / 128…（角色复杂度越高越大） | |
| **描边 outline** | `selective outline`(柔和) / `single color black outline`(复古像素) / `lineless` | |
| **明暗 detail/shading** | `high detail, soft smooth shading` / `low detail, hard shading`(块状复古) | |
| **风格后缀（固定词串）** | 每个 prompt **结尾固定**贴这串，锁定画风。例：柔和动漫=`soft smooth shading, no black outline, clean lineart`；复古像素=`crisp pixel art, bold black outline, limited palette, retro 16-bit` | |
| **头身比** | chibi(2-3 头身) / 标准(5-6) / 写实(7-8)。**靠锚点决定**，描述里慎用 "small/petite"(会被理解成大头娃娃) | |
| **道具策略 props** | 武器**手持**(质量差,慎用) / **背负**(剑盾箭袋) / **融入轮廓**。复古像素可手持简易武器；高细节柔和画风建议背负 | |
| **动画集 anim set** | 需要哪些动作：idle / walk(或 run) / cast / attack / hurt / die … 按游戏玩法定 | |
| **方向数 directions** | 8 向(本作) / 4 向 / 单向(纯横版只要 east+镜像) | |
| **特效风格 FX** | 与画风一致的特效词。柔和=`vibrant anime spell effect, no black outline`；复古=`chunky retro explosion, bold outline` | |
| **归一契约** | 统一画布尺寸 + bbox + 锚点(脚底对齐线 y 值) + 全员统一身体高 | |
| **引擎接入点** | 序列帧目录约定 + manifest 字段 + 代码里注册 sprite/VFX 的位置 | |

> ⚠️ **画风后缀是最常翻车的一项**。我们的教训：柔和细腻立绘**绝不能**写 `black outline`/`low detailed`（出粗黑块状）。
> 但**反过来**——复古像素游戏**就要**写 `bold black outline`。**没有放之四海的提示词，只有匹配本作画风的提示词**。先拿锚点试 2-3 版后缀，挑最贴的固化下来。

---

## 2. 锚点与派生（一致性根基）
1. **做锚**：选一个有代表性的角色，用 `create_character`（mode v3）按风格档案精心生成；反复挑选直到头身比/画风/骨架满意。**锚的质量决定全队上限**。
2. **派生其余**：每个新角色用
   `create_character_state(character_id=<锚>, edit_description="Transform into: <该角色外观>, <风格档案固定后缀>", use_color_palette_from_reference=false)`。
   → 继承锚的骨架/头身比/画风，只换外观/配色 → 全队天然一致。
3. ❌ **不要对每个角色单独 `create_character`**：v3 会忽略 `proportions`，头身比逐个漂移 = "画风不统一"的头号根因。
4. 外观描述写**身份特征**（发色/发型/瞳色/服装/配饰/元素宝石/表情），**别写**会触发大头/对称偏置的词（"small"、"front view symmetric"）。

---

## 3. 🚦 母图姿势闸门（铁律 · 出母图后、做任何动画前必过）
> 动画的每一帧 frame0 = 母图对应方向的旋转。**母图姿势错，整套动画继承错，必返工**。所以先免费看母图旋转，合格再动画。

逐方向目视判据（按方向数裁剪）：
- **east / west = 正侧面**：只露**一条腿、一条胳膊、侧脸(一只眼)**；露出第二条腿/正胸/正脸 = 带角度，**不合格**。
- **south/north = 正前/正后**；**斜角(SE/SW/NE/NW) = 真 3/4**，不歪不偏。
- 头身比/画风与锚一致。
- 不合格 → **回 §2 锁法A 换 seed 重派生**再检测，别将就。最多滚 2-3 次。
- ⚠️ **侧面单独生成（create_1_direction_object）行不通**：无锚漂成正面矮人、有锚出糊块。老老实实重派生母图。

---

## 4. 动作生成（全部 `animate_character mode=v3`）
**镜像省额度**：左右对称的方向只生成一半，归一时程序化镜像（east↔west、NE↔NW、SE↔SW；south/north 不可镜像）。

- **idle（循环）**：`action="standing idle, breathing gently with subtle sway"`，背向斜角易抖腿——两个斜角源都生成，取脚稳者镜像另一向（见 §6 兜底）。
- **walk/run（循环）—— 先建"迈步状态"再做循环**：直接做循环会出现"南向腿朝别处、转头"。根治：先
  `create_character_state(母图, "mid-stride walking pose, one leg stepping forward, arms swinging")` 得到迈步静态，**在该状态上**做 `walking_cycle`。
  - ⚠️ **east/west 走路常漂成 3/4 正面**：在**基础母图**（其 east 旋转是干净正侧面）上单独生成一段纯侧面 `walk_side`，归一时覆盖 run 的 east/west。
  - ⚠️ **迈步状态偶发把斜角朝向画反**(SE 画成朝 SW) → 靠 §5 走路朝向闸门兜底。
- **cast/attack（一次性，通常只做正向/背向一两个角度）**：描述里**直接写技能内容**（"casting a blazing inferno"），让引擎把元素特效**烤进角色帧**。命名约定固定前缀（如招牌技 `casting…`、职业技 `class_skill…`），方便归一器自动分类。

> **并发**：PixelLab 同时在跑的 job 有**约 10 个硬上限**，塞满后新 job 静默排队/丢失。**一次只 fire ≤4 个，回收再发下一批**；下载后逐方向核帧数，**缺哪个方向就单独以 `_v2` 名重发那一个**（别整套重发浪费额度）。

---

## 5. 🚦 走路朝向闸门（组装后必过）
> 迈步状态偶发把方向画反/带角度。组装后**逐方向把 run 和 idle 并排对照**：每个方向 run 的朝向必须和 idle 一致。
- 工具：渲一张"上排 idle 各向 / 下排 run 各向"的横条（参考 `scripts/rundiag.js` 的做法，路径换成你的项目）。
- 不一致（东接西 / SE↔SW 互换 / east-west 非正侧面）→ 重生成该向 walk-state（换 seed）或水平镜像该向修正，**改到一致再上线**。

---

## 6. 自测闸门 + 兜底阶梯
1. **脚漂移闸门**：量 idle 每个方向脚部逐帧位移，全向 ≲1.5px 才算站稳（脚本如 `fd8.js`）。
   - ⚠️ **脚漂移达标 ≠ 不抽腿**：背向斜角两腿**对称交换**时质心不动、数值照样过，但肉眼乱动。**必须额外目视背向斜角序列帧横条**确认双脚钉地。
2. **兜底阶梯（别无限 re-roll）**：v3 对某些背向斜角怎么 roll 都残留位移 → **最多 re-roll 1-2 次**，还抖就上**合成呼吸**：取一张脚钉地静态帧，程序化只对上半身做垂直起伏、腿脚每帧锁死（脚漂移 0.0px，物理上不可能抽腿）。这是终极兜底。
3. **静态 sheet 目视**：idle/walk/cast 各向横条过一遍——同一个人 / 不乱腿 / 施法有特效。
4. **游戏内配对**（若引擎已接）：真实场景里触发该角色每个技能，截图验证**动作 ↔ 技能 ↔ VFX** 三者配对不串、不缺。

---

## 7. 归一化与接入
1. **主归一器**：把所有方向所有动作的帧，统一到一个**固定 bbox + 画布 + 锚点**（脚底对齐到固定地线 y），居中。idle/run 的镜像在这一步用对称表完成。
2. **全员身体高归一**：按**身体高**（脚→头顶，**排除光环/帽缨等装饰**）把全队缩放到同一值 + 留高画布（防带装饰角色被压小 / 被裁）。
   - ⚠️ **头盔/宽体/光环角色**的"头顶检测"易误判 → 提供**显式体高覆盖**参数兜底。
3. **渲染画整幅、脚对齐地线**，不裁头顶/光环。
4. 输出 `<char>_field/` 序列帧 + `manifest.json`（尺寸/bbox/方向/各动作帧数/fps），在引擎里注册 sprite。

---

## 8. 技能表现 = 两层模型（与画风无关的通用结构）
> 这是本作摸出来、可直接套到任何游戏的施法表现结构。**角色层 + 特效层 + 握手**。
1. **角色层（cast 动画）**：在角色帧里把施法动作 + 元素光烤进去（§4 cast）。贴在**角色身上**。
2. **特效层（独立 VFX burst）**：用 `create_1_direction_object → animate_object` 做独立逐帧特效（预警/命中爆炸），打在**目标身上**。风格词按风格档案的"特效风格"项。
3. **握手（动画与逻辑同步）**：战斗逻辑**瞬时算完且权威**；伤害的**视觉表现**（掉血/飘字）**缓存**，等特效的 `impactFrame`（最亮爆发帧）再一次性 flush → 画面与扣血严丝合缝。
4. 标定 `impactFrame`：脚本统计每帧亮度/不透明像素峰值，峰值帧即命中帧。
5. **配对**：每个技能 → 一段 cast 动画 + 一个 burst；在引擎里建技能 id → {cast 动画, burst 特效, 时序, 元素色} 的注册表。素材没到位时用程序化兜底特效（握手逻辑相同），到位后无缝替换、零改代码。

---

## 9. 一个角色的完整清单（复制即用）
```
[ ] 0. 风格档案已填(§1)；锚点已做且验收(§2)
[ ] 1. 锁法A 派生母图(§2)
[ ] 2. 🚦 母图姿势闸门：逐向目视合格(§3)        ← 不过就换 seed 重派生
[ ] 3. idle / walk-state→walk / cast 生成(§4)   ← ≤4 job/批，缺向单独 _v2 重发
[ ] 4. 下载核帧 → 主归一器组装(§7.1)
[ ] 5. east/west 侧面源覆盖 run(+idle)(§4)
[ ] 6. 🚦 走路朝向闸门：run vs idle 逐向对照(§5)
[ ] 7. 脚漂移闸门 + 目视背向斜角；抖腿走兜底阶梯(§6)
[ ] 8. 全员身体高归一(§7.2)；目视全队同框等高
[ ] 9. 技能两层接入 + 游戏内配对验证(§8)
[ ] 10. 构建/版本/部署 → 截图发用户验收 → 过了再做下一个
```

---

## 10. 移植到新游戏怎么做
1. 复制本文件 + 你的归一/自测脚本（`gnorm`/`uniformsize`/`fd8`/`rundiag`/合成呼吸 等，把硬编码路径与 bbox 改成参数）。
2. **填一份新的风格档案（§1）**——这是唯一需要为新游戏定制的东西。
3. 做新锚点，跑通一个角色全流程并验收，确认风格档案/闸门都对。
4. 之后逐个派生量产，方法论与闸门原样不变。
> 风格深化：同一套方法论下，靠"风格档案"的描边/明暗/头身比/特效词四项就能从复古像素切到柔和动漫、从 8 向横版切到 4 向俯视，无需改流程。
